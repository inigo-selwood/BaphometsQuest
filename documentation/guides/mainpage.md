# Baphomet's Quest Engine

Baphomet's Quest is a small SDL-based game engine foundation. It is built
around a scene-rooted node tree, a timed resource cache, and a runtime service
root that owns shared managers.

Start with the guide pages when learning the shape of the engine:

- @ref node_tree
- @ref scene_loading
- @ref resource_cache
- @ref runtime_lifecycle
- @ref signals
- @ref maps_and_tilesets

The API reference is generated from Doxygen comments in `source/`. Use the
guide pages for intent and the class reference for exact methods, types, and
properties.
